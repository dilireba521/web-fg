import { defineComponent, ref, onMounted, onUnmounted } from 'vue'

export default defineComponent({

  setup(props, ctx) {
    return () => (
      <div>
        投资观察页面
        <br />
      </div>
    )
  }
})
