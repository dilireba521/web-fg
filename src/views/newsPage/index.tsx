import { defineComponent, ref, onMounted, onUnmounted } from 'vue'

export default defineComponent({

  setup(props, ctx) {
    return () => (
      <div>
        资讯公告页面
        <br />
      </div>
    )
  }
})
