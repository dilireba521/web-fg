import { defineComponent, ref, onMounted, onUnmounted } from 'vue'

export default defineComponent({

  setup(props, ctx) {
    return () => (
      <div>
        新闻详情页面
        <br />
      </div>
    )
  }
})
